# Brain-MRI latent-diffusion anomaly detection — pinned to the working env.
# Install:  python -m pip install -r requirements.txt
# (PyTorch with CUDA: install the build matching your CUDA from pytorch.org first.)

torch==2.12.0
diffusers==0.38.0
huggingface_hub==1.18.0
safetensors==0.8.0rc1
accelerate>=0.30        # enables low_cpu_mem_usage on from_pretrained (helps 8 GB GPUs)

numpy==2.4.6
scipy==1.17.1
scikit-image==0.26.0
scikit-learn==1.9.0

nibabel==5.4.2
matplotlib==3.10.9
tqdm==4.68.1
